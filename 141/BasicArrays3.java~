// Fri Apr 13th, 2018 lUcKy

import java.util.Random;

public class BasicArrays3 {
    public static void main(String[] args) {
        int[] nmbrs = new int[1000];
        Random rand = new Random();
        int pickedNmbr = rand.nextInt(99) + 10;
        
        for (int i = 0; i < nmbrs.length; i ++) {
            nmbrs[i] = pickedNmbr;
            pickedNmbr = rand.nextInt(99) + 10;
            System.out.print(nmbrs[i] + " ");
        }
    }
}
